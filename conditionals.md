# Conditionals
https://pugjs.org/language/conditionals.html

    - var user = false
    #user
        if user
            p Пользователь
        else
            p Аноним

Также можно использовать оператор отрицания `!`, например `!user`.
